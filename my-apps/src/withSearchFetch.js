import React from "react";

const withSearchFetch = Component =>{
    class MyComponent extends React.Component
    {
        render()
        {
            <input name="name" onChange={(e)=>{this.setTempurlValue(e)}}></input>
        }
    }
}